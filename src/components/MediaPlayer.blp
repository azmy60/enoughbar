using Gtk 4.0;

template $MediaPlayer: Overlay {
    Image {
        file: bind template.art;
        hexpand: true;
        vexpand: true;
    }

    [overlay]
    Box {
        Label {
            label: bind template.label;
        }
    }
}
